version: 2
jobs:
  code_analyze:
    docker:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - run:
          name: Download SonarQube
          command: curl --insecure -OL https://sonarsource.bintray.com/Distribution/sonar-scanner-cli/sonar-scanner-2.6.1.zip
      - run:
          name: Extract SonarQube
          commanad: unzip sonar-scanner-2.6.1.zip
      #- run:
      #    namme: Run SonarQube scan
      #    command: ./sonar-scanner-2.6.1/bin/sonar-scanner -Dsonar.projectKey=SQL -Dsonar.organization=$ORG -Dsonar.sources=. -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$TOKEN


workflows:
  version: 2
  test:
    jobs:
      - code_analyze